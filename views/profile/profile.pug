extends ../layouts/feed

block content
  .row
    .col-12.col-md-6.col-lg-4
      .card
        .card-body
          img.d-block.mx-auto.mb-3.rounded-circle.shadow(src=user.avatarUrl, alt=user.username)
          h2.h5= user.displayName
          p.text-secondary= user.username
